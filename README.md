This project is a Movie Recommendation System deployed on Oracle Cloud Infrastructure (OCI).
It uses machine learning to recommend movies based on user input, movie similarity, and metadata.
The backend runs on OCI Compute/Functions, while the dataset and model files are stored in Oracle Object Storage.
